# AI 泡沫監控儀表板 · 維護說明

> 規格看 `AGENT_BRIEF.md`。這一份放**怎麼改、踩過什麼坑、為什麼是現在這樣**。
> 最後修訂 2026-08-17

---

## 1. 快速入口

| 我想… | 去哪 |
|---|---|
| 看系統現在健不健康 | `python3 healthcheck.py`（唯讀，不碰 git） |
| 知道模型長怎樣、權重多少 | `AGENT_BRIEF.md` 第 3–4 節 |
| 改資料源或加指標 | `AGENT_BRIEF.md` 第 5 節 ＋ 本檔第 2 節流程 |
| 排查某天沒更新 | 本檔第 4 節「已知的坑」 |
| 知道某個設計為什麼是這樣 | 本檔第 6 節 |
| 動排程 prompt | `AGENT_BRIEF.md` 第 8 節 ＋ 本檔第 2 節 |

網站 <https://gundamnboy.github.io/ai-bubble-monitor/>｜repo `GunDamnBoy/ai-bubble-monitor`

---

## 2. 標準修改流程

1. **先確認工作副本與 `origin/main` 同步**（已 clone 過就 `git -C /tmp/bubble pull --ff-only`），再跑 `healthcheck.py` 記下現況（改壞了才知道是不是自己弄的）。在過期的 clone 上判斷現況，結論會整份錯——這件事發生過。
2. 改 `AGENT_BRIEF.md`（規格）。
3. 改 `scripts/update_data.py`（引擎）。動到 `to_quarters`／`pw`／`vix_score`／`bucket`／`gsy_stats`／新聞解析時，**一定要跑 `python3 scripts/update_data.py --selftest`**。
4. 動到 `data.json` 結構時，走 `AGENT_BRIEF.md` §6 末的**「幾處一組」**——那份清單是正本（目前五處：brief §6 的 schema、`update_data.py`、`index.html` 的 render 函式、內嵌離線快照、`healthcheck.py` 的硬寫常數），**這裡不重抄，因為它每次都在長**。
5. 只在**流程或人機分工改變**時才動每週排程 prompt。**先讀現有全文**：`mcp__claude-code-remote__list_triggers` 找「AI 泡沫監控：每週質化覆核與發布（v2）」，輸出很大、可能超過 token 上限而被存成檔案，那就用 Python 解析——結構是 `{"data": [ ... ]}`，prompt 在 `job_config` 底下。改用 `mcp__claude-code-remote__update_trigger` 送回。**沒有這個工具的工作階段就做不了 brief ↔ prompt 的比對，照實回報，不要憑印象比。**
   **`prompt` 是整份取代，不是局部編輯**——送出前確認所有段落都帶上了，漏掉的段落等於刪除。
6. 在 `AGENT_BRIEF.md` 第 10 節加變更紀錄，**寫清楚為什麼改**；事故經過與被否決的選項寫進本檔第 6 節。
7. **交付與發布（2026-08-10 起，見 §3）**：雲端工作階段推不了這個 repo，改動完成、healthcheck FAIL=0 之後，commit 到 /tmp 的 clone → `git format-patch -1 --stdout` 產出 patch → SendUserFile 交給使用者本機套用推送（只改 `data.json` 的走 `bubble-publish`，動到程式或文件的走 `git am`）。
8. 使用者推送後**代為驗證線上**（維護工作階段做得到，覆核排程交付完就結束；2026-08-10 前的文件與變更紀錄把這一步稱為「第 7 步」——當時交付步驟還不存在）：用 WebFetch 抓 `data.json`（**不能用 `curl`**，本容器連不到 `github.io`），比對 `meta.built` 與 `composite`。
   **拿到舊值時不要立刻判定 Pages 沒重建**——`data.json` 有 15 分鐘快取，而**目前沒有任何已驗證有效的 cache-buster**（`?t=` 無效、多打斜線也無效，見第 4 節最後三條）。正解是**去抓這次推送裡改過的另一個檔案**（例如 `MAINTENANCE.md`、`AGENT_BRIEF.md`），那個 URL 從沒被抓過、不可能有快取；它若是新版，站台就已經好了，`data.json` 只是你這一端看到的舊拷貝。
   **但那種乾淨 URL 是耗材，一個檔名只能用一次。** 推送後**先等 60–90 秒**再抓第一張，抓早了等於在站台還沒好的時候把票用掉；第一張是舊的就換第二個改過的檔名，別重抓同一個。詳見第 4 節第 3 點與第 6.10 節。

改完當下再跑一次 `healthcheck.py`，並確認沒有製造新的不同步。

**寫進文件或交給使用者的每一行指令，都要在寫的當下實際跑過一次**——這條規矩在第 6 節記過三次事故（`curl` 連不到、`?t=` 不是 cache-buster、多斜線也不是），2026-08-17 又添一次比較輕的：交給使用者的指令末尾帶了 `# FAIL 要 0` 這種註解，而**互動式 zsh 預設不吃 `#` 註解**，於是 `tail -3 # FAIL 要 0` 去找一個叫 `#` 的檔案、broken pipe，healthcheck 整個沒跑到，而畫面看起來像跑完了。**要嘛把說明寫在指令外面，要嘛不要有註解。** 同一次也發現本檔寫的是 `python`，而使用者的 Mac 上只有 `python3`。

---

## 3. 部署與發布（2026-08-10 改制，見 §6.11）

- 日常資料更新走 GitHub Actions，**不需要人介入**。
- **雲端工作階段（覆核排程、維護、Cowork）對本 repo 只有讀、沒有寫。** clone／fetch 走公開 URL 即可：
  `git clone --depth 1 https://github.com/GunDamnBoy/ai-bubble-monitor.git`
  push 一律被平台的 git proxy 擋（403「not in this session's authorized repository set」），**自備 PAT 也無效**——proxy 在 GitHub 看到憑證之前就先擋（2026-07 平台變更，上游回報 anthropics/claude-code#76248）。**不要重試、不要繞路、不要索取或使用 token**；原存於排程 prompt 的 PAT 已於 2026-08-10 移除。GitHub API 的 repo 端點同樣被擋，要看 repo 狀態就用 git。
- **發布路徑＝人機協作**：雲端產出檔案 → SendUserFile → 使用者本機推送。
  - 只改 `data.json`（每週覆核）：交付 `data-YYYY-MM-DD.json`，使用者跑 **`bubble-publish`**——他 `~/.zshrc` 裡的 zsh 函數，在 `~/Projects/ai-bubble-monitor` 的 clone 內抓 `~/Downloads` 最新的 `data-*.json` 蓋掉 `data.json`、跑 `healthcheck.py` 當關卡、commit（`data: weekly qualitative review YYYY-MM-DD`）、push。**檔名格式要對得上它的 glob。**
  - 動到程式／文件（維護改動）：交付 `git format-patch` 的 `.patch`，使用者 `git am *.patch && git push`。
  - 使用者本機用 gh 憑證推送，會正常觸發 Pages 重建（不需要 workflow 那個 `POST /pages/builds`，那是給 GITHUB_TOKEN 的）。
- 手動觸發一次完整更新：repo → Actions → `update-and-deploy` → Run workflow（人在瀏覽器前）。

---

## 4. 已知的坑

**「網站沒更新」先分三種情況判斷，不要直接改程式：**

1. `data.json` 在 repo 裡也是舊的 → Actions 沒跑或整批失敗，去看 Actions log。
2. `data.json` 在 repo 裡是新的、網站是舊的 → **Pages 沒重建**。先確認 workflow 最後那個 `POST /pages/builds` 步驟還在、而且沒有失敗。
3. 兩邊都新、只有你看到的是舊的 → 快取。**分清楚是哪一層的快取**：
   - **瀏覽器**：硬重新整理（Cmd/Ctrl+Shift+R）。加 `?v=時間戳` 一般也有效，但**這一條沒有實測過**——照本節最後一條的教訓，沒實測過的繞法不要當成保證。
   - **WebFetch／Pages 邊緣**：**目前沒有已驗證有效的 cache-buster。** `?t=` 無效（快取鍵忽略 query string）、多打斜線也無效（路徑會被正規化，`//data.json` 與 `///data.json` 是同一個鍵）。兩條都實測過，都不管用，見本節最後三條。
   - **唯一可靠的做法是換一個檔名**：去抓這次推送裡改過的**另一個檔案**（`MAINTENANCE.md`、`AGENT_BRIEF.md` 都可以），那個 URL 從沒被抓過。它是新版 → 站台已經好了，你看到的舊 `data.json` 只是快取。
   - **乾淨 URL 是耗材，一個檔名只能用一次**——但**理由不是 Pages 快取**：2026-08-17 實測，同一個工作階段內重抓同一個 URL 會被抓取工具自己擋掉（回「Already fetched … deduplicated for up to 3600s」），根本送不出請求。所以期限是**一小時、以工作階段為範圍**，跟 Pages 那 15 分鐘無關；換一個工作階段，同一個檔名可能又能用。所以：**推送後先等 60–90 秒**再抓第一張票；第一張是舊的就**換第二個這次改過的檔名**，不要重抓同一個；這次改過的檔名全部都是舊的，才輪到懷疑 Pages。理由見第 6.10 節補記。
   - **人在瀏覽器前**可以直接去 repo 的 Actions 頁看 `pages-build-deployment` 有沒有轉綠，那比猜快得多。但**覆核工作階段做不到**——它的 Bash 只通得到 git 端點，拿不到 Actions 的執行狀態。
   - 真的要看 `data.json` 本身的新內容，就等 15 分鐘，或直接 `git pull` 看 repo。

**其他反覆出現的坑：**

- **圖表卡在上一季不是壞掉**。公司開財報會 ≠ 10-Q 進 EDGAR，中間有數天到數週。有 ≥3/5 家申報就會出現空心點的「初步」季度；不到 3 家就是照設計在等。
- **`meta.lastAutoRun.fail` 出現 AAII／TAIFEX 是已知狀態**，不是新事故。看到它們不用緊張，看到別的才要查（`healthcheck.py` 的 `KNOWN_FAIL` 就是這份白名單，新面孔會直接 FAIL）。**CBOE 也在白名單裡**——它是時好時壞（之前多次失敗，2026-08-04 那次成功），所以看到 `senti` 卡片上的來源時多時少是正常的，不是有人動了引擎。
- **白名單的「反向」現在有機器在看**。`meta.lastAutoRun.streak` 記每個來源的連續成功次數，某個白名單來源連續成功 ≥15 次（約三週）healthcheck 就會 WARN，提醒把它從 §9 與 `KNOWN_FAIL` 一起移除。在這之前，「某來源其實早就修好了」只能靠人記得，而沒有人會記得。
- **`elec_rel`／`tw_margin` 顯示「序列累積中 n/21」是正常的**，這兩項要靠累積 21 個交易日才算得出來。引擎每次執行寫一筆，寫的是**前一個交易日**的值（所以永遠落後一天），一週約 5 筆。序列的起算日看 `tw.idx_hist`／`tw.margin_hist` 的第一筆，不要記在腦子裡。
  - **`tw_margin` 自 2026-08-17（v2.1.6）改為回補**：`MI_MARGN` 吃 `date=`，`backfill_margin_hist()` 在筆數不足時往回抓。**我們等了兩次才發現不必等**——引擎其實一直用 `date=` 在往回找交易日，只是沒想到同一招可以拿來補歷史。這是「以為做不到、其實沒試過」，跟第 6.10 節那三次「以為有效、其實沒驗過」是同一枚硬幣的兩面。
  - **回補完成的那天 `tw.heat` 會跳一次，而那一跳是我們按下去的，不是市場動的。** 回補前 `margin_hist` 11 筆、籌碼子群 `null`、分母 0.8、`tw.heat` 49.2（2026-08-17 的 healthcheck 預估補齊後約 46.4／49.4／52.8 三個情境）。翻象限軌跡看到這一天的跳動時，先回來看這一條。
  - `idx_hist`（`elec_rel` 用）**沒有**對應的回補，它走的是 openapi 當日快照端點；要回補得改走 RWD 的 `?date=` 那條路，還沒做。
- **某組子群從 `null` 補齊的那一天，`tw.heat` 會不連續地跳一下——那不是壞掉。** `tw.heat` 把 `null` 的子群剔除後重新歸一，補齊時分母跳回 1.0、那組整份權重一次生效。**方向可上可下**，取決於新進來的分數比現有加權平均高還是低。
  - 最常遇到的是**籌碼**：它是四組裡**唯一的單點子群**（只有 `tw_margin`），該項 `null` 整組就沒了。`healthcheck.py` 每次都會印出目前有哪些單點子群，不必靠記憶。
  - **當下缺哪幾組、補齊後 heat 大概落在哪，跑一次 `healthcheck.py` 就有**——分母 ≠ 1.0 時它會 WARN 並附三個情境的模擬值。**這裡刻意不寫死數字與預估日期**：heat 每天在動，今天算的模擬值到補齊那天早就不是這個數（WARN 也因此寫「屆時**約**為」），寫進文件只會變成下一顆過期的炸彈。
  - 這條坑在籌碼補齊後 WARN 會自動消失，但**這段文字不會**——它描述的是通則，不是當下狀態，所以可以留著。
- **`healthcheck.py` 的「今天」是台北日，不是容器的 UTC 日。** 這條是修過的坑：UTC 容器在台北 00:00–08:00 那個窗口，會把引擎當日寫的 `asof` 判成未來日期而 FAIL。改 healthcheck 的日期比較時不要退回 `utcnow()`。
- **使用者的 Mac 沒有 `node`**，所以 `healthcheck.py` 的 JS 語法檢查在本機那條路上永遠是 WARN（不是 FAIL、不擋 `bubble-publish`）。**動到 `index.html` 時，語法關卡只有雲端工作階段擋得住**——別以為本機跑過就安全。
- **EDGAR 403** 幾乎都是 User-Agent 不符 SEC 規範（要含聯絡 email）。
- **EDGAR 某公司某科目突然沒資料**，先查標籤是不是換了（GOOGL 就從 `RevenueFromContractWithCustomerExcludingAssessedTax` 換到 `Revenues`）。
- **價格抓不到**：三層備援 yfinance → Yahoo raw → Stooq。Stooq 在 Actions runner 被擋是常態，它只是最後一層。
- **不要在覆核工作階段重抓自動指標**。覆核容器的 Bash（`curl`／`requests`）只通得到 `github.com` 與 `raw.githubusercontent.com`，FRED、Stooq、SEC、TAIFEX 一律連不上，所以在那裡跑不動引擎；而唯一通得到外網的 `WebSearch`／`WebFetch` 讀 CSV／JSON 端點只會拿到 binary 亂碼，讀網頁則是經過摘要的文字，都不能代替引擎的數值抓取。硬要重抓的結果就是抓到空值或殘值，然後把每日管線的好值蓋掉。詳見 `AGENT_BRIEF.md` §8.3 的兩條路對照表。
- **推送後的線上核對不能用 `curl`**（做這件事的是維護工作階段——覆核排程自 2026-08-10 起交付完就結束，不做線上核對）。`gundamnboy.github.io` 從覆核容器的 Bash 連不到（回 http=000，不是逾時也不是 404，是連線直接被擋），舊版排程 prompt 寫 `curl … github.io/data.json | python3 -c …` 那一步**從來沒有真的執行成功過**——它會拿到空字串然後 JSONDecodeError，而流程往下走看起來像沒事。正解是用 WebFetch 讀同一個 URL 請它回報 `meta.built`／`composite`／`regime`。這是「文件寫了一個沒人驗證過的指令」的典型案例：**寫進流程的每一行指令，都要在寫的當下實際跑過一次。**
- **而 `?t=` 時間戳不是 cache-buster**（2026-08-04，上一條的續集，也是同一個教訓犯第二次）。發現 curl 不通之後改寫成「WebFetch＋`?t=` 繞開 15 分鐘快取」，只驗證了 WebFetch 抓得到，**沒有驗證那個 `?t=` 真的會繞開快取**。結果推送後連續五次換時間戳、橫跨 26 分鐘，全部拿回推送前的舊 JSON，一路誤判成「Pages 沒重建」，還跑去翻 workflow 的 `POST /pages/builds` 找兇手。快取鍵忽略 query string。識破的關鍵動作是**去抓一個不可能有快取的 URL**（同一次推送新寫的 `README.md`），它是新版的，才確定站台早就好了、舊的是快取。**分不出「站台是舊的」和「你看到的是舊的」時，找一個從沒抓過的路徑。**
- **而「多打斜線」也不是 cache-buster**（2026-08-04，上一條的續集，同一個教訓犯第三次，詳見第 6.10 節）。上一條當時的結論寫成「有效的繞法是換路徑，多打斜線即可」——那是**事後歸因**：當時換路徑之後拿到新版，功勞就記給了斜線，但真正起作用的是換了**另一個檔案**。v2.0.2 推送後實測：`/data.json`、`//data.json`、`///data.json` 連抓三次全是舊的，路徑被正規化成同一個快取鍵。**目前這條路上沒有任何已驗證有效的 cache-buster，只有「換檔名」這一招。**

---

## 5. 待辦與觀察中

- **Excel 版還停在 v1**。`UpdateAll()` 巨集的 v1.1 修正（QueryTables 文字匯入備援，解 Mac 端 Stooq 無副檔名導致 `Workbooks.Open` 失敗）**使用者尚未實測**。要不要跟進 v2 架構待決。
- **`senti` 的輸入不穩**。AAII 持續被擋、CBOE 時好時壞（卡片的 `sub`／`dir`／`src`／`url` 由引擎依當次成功的來源生成）。**2026-08-10 已接入 CNN Fear & Greed 當第四輸入**（`production.dataviz.cnn.io` 的 graphdata 端點，0–100 直讀；WebFetch 端驗證過活著）——Actions runner 通不通看 `meta.lastAutoRun.streak` 的「CNN FearGreed」：連續成功 ≥15 次就把它從 §9／`KNOWN_FAIL` 退場；持續掛零就換下一個候選（FINRA 融資餘額、CBOE 官方 CSV 端點）。
- **美國商務部（Census）資料中心營建支出**尚未納入，仍值得補——L3 少數能月頻反映實體投資的序列。**2026-08-10 已勘查端點**：`api.census.gov/data/timeseries/eits/vip`，變數為 `cell_value`／`category_code`／`data_type_code`／`time` 等 13 個，但**連 `category_code` 的可用值清單都要金鑰才查得到**（keyless 直接回「A valid key must be included」），資料中心類別碼因此未確認——寫沒驗證過的抓取違反 §5.1 精神，故本次不接。下一步：使用者到 api.census.gov 申請免費金鑰 → 存 repo secret `CENSUS_API_KEY` → 維護工作階段先用金鑰查 `category_code` 確認資料中心類別（VIP 自 2024 起把 Data Center 從 Office 拆出獨立列示）→ 依 `AGENT_BRIEF.md` §6 末的「幾處一組」流程接入 L3（記得 `LAYER_N`、「22 項指標」、質化權重 28.9% 都要跟著改）。
- **`tsmc_weight` 每月人工更新**，容易忘。若連兩個月沒動，考慮改抓別的來源或降為季頻展示。
- **沒有回測**。整套錨點除了 `gsy_runup` 之外都沒有歷史校準，是專家判斷。這是本系統最大的方法論弱點，也是當初特地把 Greenwood-Shleifer 指標放進來的原因——它至少提供一個有機率意義的參照點。
- **質化指標佔 28.9% 權重**（L1 3.9%＋L2 15.0%＋L3 10.0%），漂移風險真實存在。防線只有「`note` 必須記錄上週分數與變動理由」這一條。加減指標會改變這個數字，改完要回頭更正 `AGENT_BRIEF.md` 第 4.5 節與這一行。
- **`index.html` 內嵌的離線退路快照需要手動重灌**。它平常沒人看得到（只在 fetch 失敗時才用），所以一放就是好幾個月；2026-08 已隨 v2 重灌過一次。`healthcheck.py` 現在比對它的 `meta.version`、v2 必要區塊是否齊全、`history` 筆數（超過 60 筆 WARN，頁面會過肥），以及**它跟現行 `data.json` 有沒有脫節到會誤導**——`meta.built` 落後超過 45 天、`composite` 差超過 5 分、或象限 `regime` 不同，就 WARN。門檻刻意鬆：快照本來就是舊的拷貝，只有舊到「離線那天使用者看到另一個故事」才算問題。

---

## 6. 事故與決策檔案

### 6.1 為什麼不照中金的四維框架

中金〈如何監測 AI 泡沫?〉（劉剛／楊萱庭，2026-07-14）的四維（需求／現金流／融資來源／外部約束）概念清楚，核心數字也經得起查——我們用 SEC EDGAR 原始 XBRL 獨立重算，2026Q1 五大雲廠 Capex/OCF = 94.0%，與原文完全吻合。**但它是為了寫一篇文章而設計的，不是為了每天量測。**

六個被我們改掉的地方：

1. **沒有估值維度**。泡沫的定義裡不能沒有價格。→ 加 `cape`／`mag7`／`nvdape`。
2. **需求指標的樣本偏誤**。用少數幾家的公開表態代表整體需求。→ 改用 RPO 合約儲備與台灣月營收這類全樣本、可驗證的量。
3. **「ROIC > WACC 所以不是泡沫」是邏輯謬誤**——泡沫期的 ROIC 本來就會虛高，因為分母（資本）的折舊還沒開始咬。→ 換成 `dnagap`（折舊成長 − 營收成長），折舊開始跑贏營收正是資本過度投入的落地訊號。
4. **漏掉循環／供應商融資**。這是本輪最像 2000 年電信設備商融資的地方。→ 新增 `circular`。
5. **外部約束過度事件化**。把政策風險寫成一件件新聞，無法連續量測。→ 改成 7 項布林觸發器（CPI、政策利率、10Y、利差…）。
6. **低估了階段推進的速度**。→ 加 `stage` 四階段與六項檢查清單，每週更新證據。

**被否決的選項**：直接照抄中金四維再補一個估值維度。否決理由——概念分類與更新頻率正交，季頻的基本面和日頻的市場擠在同一個平均裡，日頻訊號會被稀釋成一條平線。v1 上線一個月的實際表現證實了這個擔憂：綜合溫度幾乎不動。

### 6.2 2026-07-18：`data.json` 更新了，網站停在 54.1

**現象**：使用者回報網站最後更新停在 2026-07-18、分數 54.1，但 repo 裡的 `data.json` 明明是新的。

**根因**：用 `GITHUB_TOKEN` 推送的 commit 不會觸發 GitHub Pages 佈建——這是 GitHub 為了防止 workflow 互相觸發造成無限迴圈的設計，不是 bug。

**修法**：workflow 末尾加一步明確呼叫 `POST /repos/{repo}/pages/builds`，並加上 `pages: write` 權限。這一步失敗設為非致命（`|| echo`），因為資料本身已經進 repo 了。

**教訓**：「檔案有沒有更新」和「網站有沒有更新」是兩個獨立的問題，排查時要先分開確認。本檔第 4 節開頭那三種情況的判斷法就是從這次來的。

### 6.3 2026-07-18 之前：新聞永遠是舊的

**現象**：使用者連續回報「本期焦點事件沒有更新到最新的新聞」。

**根因**：原設計是每週人工挑選新聞。每週挑一次，在一個每日更新的儀表板上，有六天看起來都是舊的。

**修法**：改成每日 Google News RSS 管線，用來源權威加權＋垃圾來源黑名單＋時效衰減排序自動挑 12 條。人只在漏掉重大結構性事件時補 1–2 條。

**被否決的選項**：提高人工挑選的頻率。否決理由——這是把架構問題當成勤奮問題解。

**連帶規則**：因此 `events` 被列為每週覆核**絕對不可覆寫**的欄位。覆核工作階段用 `WebSearch` 確實找得到新聞（那條路通得到外網，見第 4 節），但那只是**當下這一次搜尋**的結果，比不上每日管線連續七天的權威加權與時效排序；用它覆寫，等於拿一次即興搜尋換掉七天的累積。人要做的是在管線漏掉重大結構性事件時**加**一兩條，不是整批重寫。

### 6.4 v1 的卡片文字與數值脫鉤

**現象**：自我檢視時發現，指標卡片上的敘述文字是 v1 建置當天寫死的，數值卻每天自動更新。一個月後兩者開始互相矛盾（文字說「接近歷史高點」，數值早已回落）。

**修法**：把會隨數值變動的敘述抽成 `sub` 欄位，由 `update_data.py` 在更新數值的同時一起產生；**自動指標的** `note` 只留不隨時間變的結構性說明。

**質化指標是例外**：它們沒有引擎可生成 `sub`，所以 `note` 反而**必須**留變動軌跡（「上週分數 → 本週分數 ＋ 理由與日期來源」，見 `AGENT_BRIEF.md` 第 4.5 節）。這是刻意的不對稱——自動指標的漂移看數值就知道，質化指標的漂移只能靠這行字查核。

**教訓**：**任何寫死的文字只要和自動更新的數字放在同一張卡上，就是一顆定時炸彈。** 新增指標時檢查：這張卡上有沒有哪句話明年還會是對的？不確定就寫進 `sub` 讓它自動生成。

**明文例外**：TAB4「覆蓋其中 4.5 項」維持人寫常數——它是框架級判斷，無法由資料生成，只隨框架改版而動。稽核到它時不要當成漂移重報一次。

**2026-08-04 補**：同樣的炸彈在 `index.html` 的信用磚塊又找到三顆（「歷史低檔」「13-18＝自滿區」「（降息中）」），都是 v1 建置當天寫死的。已改為由數值生成或刪除。**寫死的文字會躲在 render 函式裡，不在 `data.json` 裡，healthcheck 抓不到——只能靠讀。**

### 6.5 為什麼觸發器不進綜合溫度

**被否決的選項**：把 7 項觸發器折成分數併入 L2。

**否決理由**：觸發器是離散門檻，門檻附近的微小波動會讓綜合溫度來回跳，而綜合溫度的用途是看**趨勢**。連續量與離散量混在一起，兩邊的訊息都會被弄髒。分開之後，溫度負責「有多熱」，觸發器負責「有沒有跳電」，兩者在儀表板上並列但不相加。

### 6.6 為什麼指標層是等權、權重只放在層級

**被否決的選項**：給 22 個指標各自的權重欄位。

**否決理由**：22 個可調參數等於 22 個漂移面。每次「覺得這個指標比較重要」都能悄悄改一次，事後無從查核。放在層級只有 3 個數字，任何改動都很顯眼，而且**加減指標本身就是在調權重**——這個機制已經夠用了。代價是無法表達「這一層裡某一項特別關鍵」，接受這個代價。

### 6.7 前端讀得到「鍵」不等於讀得到「欄位」

**現象**（2026-08-04 自我檢視發現，已上線一段時間）：信用磚塊顯示「−NaNbp」。原因是前端算 `Math.round((sp.hy.now - sp.hy.y1) * 100)`，`charts.spreads.hy` 這個鍵存在、`now` 也存在，但 `y1` 從來沒被引擎寫過。`undefined` 參與運算得到 `NaN`，一路畫到畫面上。

同一次檢視還找到兩個相鄰的失效模式：`spreads.fedfunds`／`spreads.usinfo` 是 v1 淘汰指標的殘值，鍵還在、`asof` 停在幾個月前，前端卻當成即時值在畫；`index.html` 內嵌的 `<script id="dashboard-data">` 離線退路快照仍是 v1 六維模型，一旦 `fetch data.json` 失敗，整頁會靜靜地退回舊模型而且看不出來。

**修法**：引擎補寫 `hy.y1` 並實際抓取 `fedfunds`／`usinfo`；前端所有磚塊改成缺值就不畫那一段（`dl()` 遇到 `null`／`undefined`／非有限數一律回空字串）；退路快照以現行 `data.json` 重新產生。

**教訓**：**JSON 沒有 schema，缺欄位不會報錯，只會安靜地變成 `NaN` 或空白。** 所以這三件事都做成 `healthcheck.py` 的機械檢查——比對前端讀取的 `charts.spreads` 鍵與欄位 vs 引擎（用 AST 解析 `update_data.py`）實際寫入的、比對退路快照與 `data.json` 的 `meta.version`、偵測 `asof` 凍結。**凡是「兩個地方要一致」的規則，寫進文件只是提醒，寫進 `healthcheck.py` 才是保證。**

### 6.8 2026-08-04（第二輪）：一次比對抓不完，而且最貴的那幾顆在頁面深處

**現象**：v2.0.1 才剛做完一輪子代理比對、寫完變更紀錄，隔一次維護再叫一次子代理，又抓出十幾處。**其中兩處已經在線上自相矛盾**：TAB4 說 NVIDIA 循環投資「>$40B」，同一頁的 `circular` 卡片說「>$540B」；TAB4 說「Oracle CDS 創歷史新高」，`weakcredit.note` 說「創約 18 年高」。使用者只要多滑一頁就看得到。

**根因有兩層**：

1. **第一輪只掃了「會不會壞」，沒掃「還是不是真的」。** v2.0.1 修的是 `NaN`、缺鍵、殘值——都是機器抓得到的失效。但 TAB4 是一整頁論述文字，裡面夾著十幾個 v1 建置當天寫死的數字與判語，沒有任何一項會報錯，只會慢慢變成假的。**能報錯的問題會自己浮出來，不能報錯的要靠人主動去讀。**
2. **「改對」不等於「改好」。** 第一輪把 VIX 的「13-18＝自滿區」改成隨數值生成，是對的做法；但同一次沒有回頭問「頁面上還有哪些地方是同一個型態」。這次才發現分區界線在頁面上有**五份**寫死拷貝、觸發器項數有**三份**、檢查清單項數與台股子群權重各有兩份。

**修法**：不只改文字，把每一處寫死拷貝改成從 `data.json` 現算（`zoneSegs()`、`.trigN`、`.chkN`、`tw.subWeights`、`data-ind-sub`、TAB4 的六段判語）。同時把原本只寫在文件裡的規則變成機器檢查——`meta.lastAutoRun.streak` 讓 §9 的退場規則第一次真的可執行，另加台股子群權重三處對帳與質化頻率分級對帳。

**教訓**：**一份文件寫著「某某規則」，而沒有任何程式在檢查它，那條規則就等於不存在。** 這是 6.7 那句「寫進文件只是提醒，寫進 `healthcheck.py` 才是保證」的續集——但 6.7 講的是欄位一致性，這次講的是**維護規則本身**。§9 那條「連續數週出現在 `ok` 裡就該移除」寫了好幾週，而 `data.json` 根本沒存能判斷「連續數週」的東西。**寫維護規則的當下，要順手問一句：執行它的人拿得到判斷所需的資料嗎？**

### 6.9 2026-08-04（第三輪）：修改自己會製造下一批不同步

**現象**：6.8 那批改完、變更紀錄也寫好之後，再叫一次子代理**複驗這批改動**，又抓出十幾處——**而且是這批改動自己製造的**。其中兩個是真 bug（`senti` 的 `url` 條件只看 VIX、`evalNvdaCmp` 沒比較 `mag7` 就宣稱「遠不及集中度」），三個是「同一顆炸彈只拆一半」，四個是文件更正時在別處留下新的矛盾。

**最值得記住的三種型態**：

1. **只拆一半的炸彈**。`senti` 的 `dir`／`src`／`url` 都改成動態了，但同一張卡的 `note` 還寫著「三者等權合成」與「散戶淨空」——後者是 AAII 導出的判斷，而 AAII 正在 `fail` 裡。**修一張卡的時候要把那張卡的每一個文字欄位都看過**，不是只看你正在改的那幾個。
2. **改對了語氣、弄丟了判斷**。把 TAB4 的寫死敘述改成資料生成時，「對照 2000 年 Cisco 130×」這個歷史錨和「OpenAI IPO 是試金石」這個判斷都消失了，只剩下沒有參照物的內部溫度分。**寫死的是「數字」，該保留的是「論證」**——換掉數字來源不等於可以刪掉論證。
3. **新程式碼犯了本文件已經記過的錯**。`healthcheck.py` 的 streak 檢查寫成「抓不到就靜默跳過照印 PASS」，而 `AGENT_BRIEF.md` §4.5 早就明文寫過「舊版寫成『抓不到就跳過』，於是改寫句型可以無聲關掉比對還照樣印 PASS，比沒有檢查更糟」。**寫新檢查的時候，先去讀一遍舊檢查踩過的坑。**

**教訓**：**「改完再比對一次」不是形式，改動本身就是新的不同步來源。** 而且複驗**不能由改的人自己做**——主線改完之後自我檢視時判定「大致沒問題」，子代理獨立看卻抓出十幾處。這在三個知識庫上反覆驗證過，這次又驗證了一次。

**次要教訓**：子代理比對**不是做過一次就永久有效**。每次大改動之後都要再叫一次，而且要明確要求它去讀論述型的整段文字，不要只比對結構化欄位。

### 6.10 2026-08-04（第四輪）：「換路徑」這個繞法自己也沒被驗證過

**現象**：v2.0.2 推送後驗證線上，`data.json` 連抓三次（`/data.json`、`//data.json`、`///data.json`）全部拿回推送前的內容，一度判定 Pages 沒重建、跑去翻 Actions。實際上站台早在幾分鐘前就好了。

**根因**：**多打斜線不會產生不同的快取鍵**——路徑會被正規化，三個 URL 對抓取端是同一個。而第 4 節那條「有效的繞法是換路徑，多打斜線即可」是上一次事故的**事後歸因**：當時換了路徑之後拿到新版，就把功勞記給了斜線，但真正起作用的是**換了另一個檔案**（`README.md`）。

**這是同一個錯誤的第三次**：

| 次 | 寫進流程的繞法 | 只驗證了 | 沒驗證的 |
|---|---|---|---|
| 1 | `curl … github.io/data.json` | （什麼都沒驗，直接寫進 prompt） | 這個容器根本連不到 `github.io` |
| 2 | WebFetch ＋ `?t=<時間戳>` | WebFetch 抓得到東西 | `?t=` 真的會繞開快取 |
| 3 | WebFetch ＋ 多打斜線 | 換路徑之後拿到了新版 | 新版是**斜線**帶來的還是**換檔案**帶來的 |

三次都是同一個形狀：**驗證了「這樣做之後事情看起來對了」，沒有驗證「是這一步讓它變對的」。** 第三次尤其陰險，因為它有一次成功的觀察撐著——只是那次成功的功勞被歸錯了對象。

**修法**：第 4 節與 `AGENT_BRIEF.md` §8.3 的「換斜線」全部刪掉，改成明說「目前沒有已驗證有效的 cache-buster」。唯一保留的做法是**抓這次推送裡改過的另一個檔名**——這一招在第 2、3 次事故裡都真的救了場，是唯一有兩次獨立實測支撐的。

**教訓**：**同一份文件裡，一條靠推論寫下的規則旁邊，往往就躺著一條靠實測寫下的規則。** 這次救場的是「找一個從沒抓過的路徑」——它是被觀察到的，所以是對的；死在旁邊的「多打斜線」是被解釋出來的，所以是錯的。**分辨方法：問這條規則當初是被觀察到的，還是被解釋出來的。** 被解釋出來的那些，要嘛去實測，要嘛在文件裡標明它未經驗證。

**要驗證一個 cache-buster，唯一的方法**：在已知內容剛變過、而且**確定站台已經更新**的時候用它抓一次，看拿到新的還是舊的。單獨看「它抓得到東西」永遠證明不了任何事。

**補記（v2.0.3 推送當下踩到的續集）**：唯一可靠的「換檔名」有一個實務限制——**乾淨 URL 是耗材**。v2.0.3 推送後太急，推完幾十秒就去抓 `AGENT_BRIEF.md`，那時 Pages build 還沒完成，拿回舊版。那次推送只改兩個 `.md`，另一個 `MAINTENANCE.md` 更早已經抓過，等於手上再沒有沒被抓過的檔名。

**實測到的事實**：推送後幾十秒抓乾淨 URL 會拿到舊版。
**推論、未實測**：那一抓把該 URL 寫進 15 分鐘快取，所以它之後也不能用了。**沒有做過「等 build 完成後再抓同一個 URL，看是新是舊」這個能證成因果的觀察**——依本節上面那條分辨法，這是被解釋出來的、不是被觀察到的，在此標明未經驗證。真要確認，下次推送時等 build 完成後重抓一次剛才抓過的 URL 就知道了。

**補記二（2026-08-17，第一個真正的觀察）**：本輪推送後照流程抓第一張票（`AGENT_BRIEF.md`），拿回舊版；等 150 秒後想重抓同一個 URL 做上面那個對照——**結果根本抓不出去**，抓取工具自己回「Already fetched … 172s ago in this session … deduplicated for up to 3600s」。

- **被觀察到的**：同一個工作階段內，同一個 URL 一小時內抓不了第二次，擋在抓取端。「一個檔名只能用一次」因此在實務上成立，但**理由跟 Pages 的 15 分鐘快取無關**，而且範圍是工作階段、期限是一小時。
- **仍未被觀察到的**：Pages／邊緣那層到底有沒有把它寫進 15 分鐘快取。上面那個對照實驗**在同一個工作階段裡做不出來**——工具不讓你重抓。要驗只能換一個工作階段回頭抓同一個 URL。
- 順帶確認了流程本身是對的：第一張票拿回舊版之後換第二張（`MAINTENANCE.md`），它是新版，站台早就好了。**「第一張是舊的就換下一張，不要重抓同一個」這條不管哪個機制成立都是對的**，所以流程不用改，要改的是文件裡寫錯的那個理由。

**所以流程改成**：推送後**先等 60–90 秒**再抓第一張票；第一張是舊的就**換第二個這次改過的檔名**，不要重抓同一個（這條不管上面那個推論成不成立都是對的）；改過的檔名全部試完還是舊的，才輪到懷疑 Pages。推送若只改了少數幾個檔，可用的票就只有那幾張——**這是「乾淨 URL」這一招唯一的實質成本。**

**人在瀏覽器前另有捷徑**：直接去 repo 的 Actions 頁看 `pages-build-deployment` 有沒有轉綠。但**不要把這條寫進覆核流程**——覆核工作階段的 Bash 只通得到 git 端點，拿不到 Actions 狀態（見第 3、4 節）。這正是 §6.8 那句「寫維護規則的當下，要順手問一句：執行它的人拿得到判斷所需的資料嗎？」——本節初稿就寫了「先去 Actions 看轉綠」，複驗時才發現覆核端根本做不到。

### 6.11 2026-08-10：雲端推送被平台擋死，發布改為人機協作

**現象**：每週覆核第一次撞上 push 403——git proxy 回「not in this session's authorized repository set」，連排程裡存的 fine-grained PAT 都推不動（proxy 在 GitHub 看到憑證之前就擋）。當週改以檔案交付、使用者本機推送後照常發布，網站當日更新。

**根因**：平台 2026-07 的變更（上游 anthropics/claude-code#76248，開立時仍無官方回應）：雲端工作階段的 git 寫入被限制在授權 repo 清單內，PAT pass-through 不再放行。**讀不受影響**。錯誤訊息指的兩個解法（session sources、`add_repo`）在產品裡都不存在——查證過，不要再找。

**決策：發布改為人機協作**（詳見 §3）。雲端做研究、評分、重算、healthcheck、產檔；使用者本機 `bubble-publish`（data.json）或 `git am`（patch）推送。

**被否決的選項**：
- **改成本機排程**：全自動，但週一早上電腦必須開機在線，睡眠即無聲跳過整週——比「手動 30 秒」更容易無聲失敗，且雲端排程的研究能力（WebSearch/WebFetch）不受影響，只有最後一哩需要人。
- **等平台修復**：無時間表；等待期間網站每週停更。
- **每週手動補發 token／換 token**：根因不是 token，換一百把也一樣。
- 順帶把失效的 PAT 從排程 prompt 移除（明文躺著、又推不動，只剩風險沒有用途），並提醒使用者到 GitHub 撤銷。

**教訓**：外部平台的能力邊界是會動的——2026-08-04 §3 還寫著「HTTPS git 操作正常」，六天後這句話只對了一半（讀對、寫錯）。**依賴外部平台能力的流程，要在文件裡寫清楚「這條路哪天斷了怎麼辦」**；這次是現場即興出來的（檔案交付），現在補進 §3 變成固定路徑。

### 6.12 2026-08-17：規格書的四分之一是變更紀錄，而它每週被完整讀一次

**現象**：`AGENT_BRIEF.md` 37.7k 字元，其中 §10 變更紀錄佔 **9,565 字元（25.1%）**——而這份檔案的第一讀者是每週覆核排程，它每次執行前**完整讀一次**。也就是說每週有四分之一的閱讀量花在「2026-08-04 那天改了哪 50 行」上，而覆核要做的事一條也不在裡面。

同一份檔案的開頭第 4 行寫著「事故經過與被否決的選項寫在 `MAINTENANCE.md` 第 6 節，**不要寫進這裡**」。§10 的 v2.0.2／v2.0.3／v2.0.4 三則正是事故敘事，與本節 §6.8／§6.9／§6.10 是同一件事的第二份拷貝。**規則寫在自己身上、自己卻沒照做**，因為變更紀錄長得不像違規——它是「紀錄」，不是「規格」。

**根因**：沉積。每次維護都在 §10 加一則，加一則的成本極低、刪一則感覺有風險，於是層層疊上去。這跟 §6.4 那顆定時炸彈是同一個家族的另一種形狀：那顆是**寫死的敘述會過期**，這一顆是**正確但無關的敘述會累積**——兩者都不會報錯。

**修法**：§10 改成索引表（版本｜日期｜一句話｜→ 本節哪一小節），敘事全部由 `MAINTENANCE.md` 第 6 節承擔，逐行差異交給 `git log`。刪之前先把**只寫在 §10 裡的實作事實**撿出來搬到它該待的地方（brief §5.2）：`fcf` 的 YoY 在基期 TTM FCF ≤0 時沿用舊值、`hyoas`／`ccc` 取不到 91 天基期就 fail 而非用 0bp 假變化、`data.json` 的原子寫檔。§10 因此由 9,565 降到 1,489 字元，brief 全檔 −21.2%。

**同一輪一併修掉的六處「已被自己推翻的抄本」**：§9 的 Census 一列還寫「列為 v2.1 待辦」（現在版本就是 v2.1，§10 早已寫「本次不接」）；§10 有一則宣稱「§6.7 的引用已刪」而該引用至今還在、且 §6.7 確實有那條標準（**一則假的變更紀錄**）；§7 把交付流程指向 §8.4（正文在 §8.3，同型錯誤 v2.0.2 才修過一次）；§6 的常數清單寫「四組」卻漏了 §4.5 自己指名的 `QUAL_MAXAGE`；§4.5 的 `YYYY-MM` 例子引用 `tokens` 的 2026-05，而它早已是 2026-07-28；本檔 §5 把「五處一組」指向本檔 §6（本檔 §6 是事故檔案）。

**還有一處指錯門的 pointer**：brief §4.4 說 `rpo` 取負後的錨點「寫在 `refresh_edgar()`」，實際在 `main()` 內的 `f_rpo()`——而同一節自稱「本文件是這三個指標唯一的規格來源」、`healthcheck.py` 對它們一律跳過。**唯一的規格來源指錯位置，比沒有指標更糟**：讀的人會照著去看一個沒有那段程式碼的函式，然後以為自己找錯版本。

**被否決的選項**：
- **把 §10 整段刪掉不留索引**。否決理由——「哪一版動過什麼」在追一個舊行為為什麼長這樣時仍然有用，而一行索引的成本接近零。
- **把 brief 拆成「覆核用規格」＋「維護者參考」兩檔**（§5 管線、§7 workflow、§1 拓撲對每週覆核都是 reference 不是 step，約再省 14%）。否決理由——排程 prompt 的第一步是「完整讀一次本檔」，拆檔就必須同步改 prompt，而排程 prompt 是**整份取代**（§2 第 5 步），與文件瘦身放同一輪，出錯時分不出是哪一邊造成的。本輪已取得 prompt 全文並做完 brief ↔ prompt 比對（結果相符、章節指向全部有效），拆檔留到下一輪單獨做。

**教訓**：**一份被排程每次完整讀一次的文件，它的長度就是每週的固定成本，而變更紀錄是唯一會單調成長的那一塊。** §6.4 教我們「寫死的文字會過期」，這一節補上另一半：**正確而無關的文字不會過期，它只會愈長愈厚，然後把真正要讀的東西稀釋掉**。定期問一句：這一節如果整段不見，這週的覆核會做錯什麼嗎？答不出來的那幾節就是索引化的候選。

### 6.13 2026-08-17：把關的工具只在它自己長大的那台機器上跑過

**現象**：使用者第一次在自己的 Mac 上跑 `healthcheck.py`，**整支當掉**，連第一行輸出都沒有。`Ctrl-C` 打出來的 traceback 指向 `find_repo()` 的 `os.walk`——它在挑候選路徑時，不管有沒有現成答案，都先把 `/sessions` 和整個家目錄走一遍（深度 4 層）。雲端容器的家目錄幾乎是空的，這段零成本；使用者的 Mac 家目錄底下有 iCloud、Downloads、Library，於是它就停在那裡。`--repo` 參數也救不了——**候選比對排在掃描之後，等於形同虛設**。

**為什麼這條特別貴**：`healthcheck.py` 是把關每週交付的工具（FAIL 必須是 0），而 `bubble-publish` 的關卡**正是在使用者的 Mac 上跑它**。也就是說，這支工具在它最該發揮作用的那台機器上是掛的，而我們一直沒發現——因為寫它、改它、跑它的一直是雲端工作階段。

**修法**：`find_repo()` 先查便宜的候選（`--repo` → 本檔所在目錄 → 幾個慣用路徑），命中就 return；`os.walk` 降為最後手段。順便把 `~/Projects/ai-bubble-monitor` 加進慣用路徑。

**同一天發現的第二件事**：這台 Mac 沒有 `node`，所以 JS 語法檢查在本機永遠是 WARN。它不擋交付是對的（本機那條路的用途是驗 `data.json`），但**動到 `index.html` 時，語法關卡只剩雲端那一份**，已寫進第 4 節。

**`fresh` 這個死欄位：先記載，同日再修活。** 引擎原本一律寫 `"ok"`、從來不寫 `"stale"`，而前端有兩處據此畫「⚠ 資料延遲」，TAB2／TAB5 還對使用者承諾了這個標示——等於承諾了一個永遠不會發生的東西。修活的難處不在程式，在**得先定義每個指標的過期門檻**（那是新規格不是修 bug），所以先在 brief §6 註明它是死的，把「頁面在說謊」降級成「文件有記載」。

同日 v2.1.4 接著做完：`IND_MAXAGE` 依自然更新頻率分組（日頻 10 天／季頻 130 天／質化沿用 §4.5 的 21-75-130／未列 45 天預設），`set_fresh()` 在寫檔前重標，`healthcheck.py` 用**引擎自己那份門檻與 `asof_date`**（把函式原始碼挖出來執行，不另寫第二份解析器——自己再寫一份就是又開一個漂移面）重算對帳，不一致 FAIL。

**門檻刻意訂寬**：日頻那 10 天不是「一天沒更新就叫」——連假加上來源當天沒發布，5–6 天是常態。訂完當下用現行 `data.json` 試算過：**22 項全部落在門檻內、沒有任何一項變 stale**。那正是想要的結果——徽章只在真的卡住時出現，而不是上線當天先嚇使用者一跳。

**教訓**：**工具的執行環境是規格的一部分。** §6.8 問的是「執行它的人拿得到判斷所需的資料嗎」，這一節問的是它的前一半——**執行它的機器，跟你寫它的那台是同一種嗎？** 一個只在單一環境跑過的把關工具，等於還沒被驗證過。
